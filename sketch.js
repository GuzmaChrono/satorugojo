function setup() {
    createCanvas(400, 400);
  }
  
  function draw() {
    background("blue");
    rect(0, 0, 100, 150);
    fill ("red");
    rect(mouseX, mouseY, 20, 35);
   // console.log( mouseIsPressed)
    If (mouseIsPressed)
  }
  function setup() {
    createCanvas(400, 400);
    background("white")
  }
  
  function draw() {
    stroke("blue");function setup() {
    createCanvas(400, 400);
  }
  
  function draw() {
    background("blue");
    rect(0, 0, 100, 150);
    fill ("red");
    rect(mouseX, mouseY, 20, 35);
   // console.log( mouseIsPressed)
    If (mouseIsPressed)
  }
  function setup() {
    createCanvas(400, 400);
    fill("red");
    
    
    if (mouseIsPressed) {
      rect(mouseX, mouseY, 20, 35);
    }
  }
  